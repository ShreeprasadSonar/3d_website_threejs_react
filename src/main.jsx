import ReactDOM from 'react-dom/client';
import App from './App';
import './index.css';


ReactDOM.createRoot(document.getElementById('root')).render(<App />); // React is goign to hook into a single div on our HTML (root) and populate it with conent in App
